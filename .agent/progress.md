# 作業履歴

> **書き方**: 作業が一段落したら**追記**する（上書きしない）。
> 1 エントリ **1〜3 行**に収める。詳細は git log とコミットメッセージに任せる。
> 単発の質問やタイポ修正では更新しない。
>
> 古いエントリが 30 件を超えたら `progress-archive.md` に移す。

---

- 2026-08-24: CODIHA 2026 の提出要件を `課題/2026-09-09_CODIHA2026_提出要件.md` に抽出。
  締切（コード 9/9 17:00・プレゼン資料 9/16 11:50）・審査基準 5 項目・0 点条件が正本。
- 2026-08-24: hackathon-template 由来の残骸（マイコン・回路・LaTeX 報告書向けの
  ディレクトリ一式と専用 CI）を削除。README / AGENTS.md / .agent / docs / tools の
  記述を Docker・オープンデータ前提に同期。削除対象の内訳はコミット 00bf09d を参照。
- 2026-08-24: 提出単位として `app/` を新設（規約のみ・実装はまだ無い）。
  `.gitignore` に zip / 7z・フレームワークのキャッシュ・`app/readme.txt` を追加。
- 2026-08-24: 主催者指定の 2 ソースから `data/` に 16 本取得（千葉県 Excel 8・市川市 CSV 8）。
  ライセンスは PDL1.0 / CC BY 4.0 でいずれも出典明記で再配布可。`SOURCE.md` に諸元と落とし穴を記録。
- 2026-08-24: `data/analysis/` に探索的分析を追加（スクリプト 5 本・グラフ 9 枚・`findings.md`）。
  市川市は町丁字で高齢化率が 6.6 倍ちがう／避難場所は全地区 839m 以内／AED が 0 箇所の地区が 96。
  クリーン venv（Python 3.12・3.14）で同一 PNG が出ることを確認済み。
- 2026-08-24: `app/` に地図アプリの骨格を実装（Next.js 16 + MapLibre GL 6 + Tailwind v4）。
  市川市の避難場所 123・AED 304・子育て施設 388 を地図に重ね、レイヤー切替とポップアップ、
  OSRM による徒歩ナビ（Geolocation 拒否時は地図クリックで出発地点指定）まで。
- 2026-08-24: `docker compose up` で起動を実機確認。**MapLibre v6 は worker が別ファイルになり、
  バンドルすると読めない**ため `public/maplibre/` に配置して `setWorkerUrl` で指す
  （`app/scripts/copy-maplibre-worker.mjs`）。これが無いと点が永久に出ない。
- 2026-08-24: 市川市 CSV → GeoJSON 変換を `data/scripts/build_geojson.py` に追加。
  避難場所 CSV 末尾の 6 行は「緯度経度の欠損」ではなく**全列が空の行**だったので SOURCE.md を訂正。
- 2026-08-24: 提出アーカイブの生成と検証を `tools/package_submission.sh` に自動化。
  gitignore 判定で `app/` をクリーンコピー（readme.txt だけ強制同梱）→ 7z 化 → 展開し直して
  必須ファイル・BOM・日本語名・キャッシュ混入・compose を検査。NG ならアーカイブを消して非 0 終了。
- 2026-08-24: `tools/package_submission.sh` の compose 操作を `-p pkgtest-<PID>` で隔離。
  `compose.yaml` の固定プロジェクト名を同じマシンで取り合って他人のコンテナを潰す事故を防ぐ。
  あわせて `--smoke` で展開先を実起動して HTTP 200 まで見る検査を追加（後始末は trap で保証）。
- 2026-08-24: プロダクトを **CHIZUBA**（千葉の地図系サービスを束ね、住民と行政が相互投稿できる
  ウェブサイト）に決定。要件定義を `docs/design/requirements.md` に新規作成（機能 F-1〜F-8・
  ロール 3 段階・画面 S-1〜S-7・実装順序 P1〜P8・提出要件との対応）。
- 2026-08-24: 設計判断 — 危険箇所/浸水/観光おすすめを **1 テーブル（`category` + `details` JSON）に統一**。
  DB は **PostgreSQL 17（DOI）+ healthcheck**、**PostGIS は使わない**（DOI 優先・矩形検索と距離で足りる）。
  認証は **Google OAuth 本線 + キー未設定時デモログイン**（審査員が `docker compose up` だけで投稿まで試せる）。
- 2026-08-24: `interfaces.md` をテンプレから実仕様へ全面記入（つなぎ目 I-1〜I-9）。
  投稿 API・気象 API・DB・セッション・ハザードタイルの形式と**異常時の約束**を確定。
- 2026-08-24: ハザードタイルと気象庁 JSON の疎通を実測。洪水/高潮/津波は市川市 z=14 で 200、
  土砂は 404（市川市に該当区域が無い）。**タイル無し＝安全ではない**ので凡例で明示する方針。
- 2026-08-24: `.gitignore` に `.env.local` / `.env.*.local` を追加。Next.js は `.env` 以外も既定で読むため、
  `.env` だけの除外では public リポへのシークレット混入を防げなかった。
- 2026-08-24: **P1 完了**。ハザードマップ（洪水・高潮・津波の浸水想定）を `app/src/lib/hazards.ts` +
  `HazardLegend.tsx` で実装。レイヤーごとの ON/OFF と不透明度・凡例・出典が動く。
  Docker 起動・`package_submission.sh --smoke`・ブラウザ実操作まで確認済み。
- 2026-08-24: 実測でわかったこと — **浸水深の凡例は洪水 6 段階／津波・高潮 8 段階で別物**
  （タイルの画素を千葉県内 20 枚ぶん数えて確認。同じ色でも表す深さが違う）。
  タイル配信は z=2〜17 だが、**引きすぎた縮尺で無駄な 404 が出る**のでレイヤーに minzoom 6 を付けた。
- 2026-08-24: 同一ワークツリーで 2 セッションが git を触り、HEAD を取り合う事故が発生。
  **並行作業は `git worktree` で必ず別ディレクトリに分ける**（共有チェックアウトは HEAD を共有する）。
- 2026-08-24: P2 として DB と認証を実装（`feat/p2-db-auth`）。`compose.yaml` に
  `postgres:17-alpine` + `pg_isready` の healthcheck + `depends_on: service_healthy` を足し、
  `app/db/init/*.sql` で 5 テーブル（市町村マスタ・ユーザー・投稿・写真・コメント）を作る。
- 2026-08-24: 認証は Auth.js の JWT セッション。`GOOGLE_CLIENT_ID/SECRET` が両方あれば Google
  モード、無ければ**自動でデモログイン**（表示名 + 一般／行政の選択）。判定はサーバー側で 1 回。
- 2026-08-24: 実測で判明 — コンテナ内では Auth.js が自ホストを `0.0.0.0` と判定し、Google への
  `redirect_uri` が `http://0.0.0.0:3000/...` になる。`compose.yaml` で `AUTH_URL` を固定して解決。
- 2026-08-24: `tools/package_submission.sh` の起動テストがサービス一覧の先頭（＝ `db`）を掴む
  ようになっていたので、**ホストにポートを公開しているサービス**を選ぶよう修正。
- 2026-08-24: **P3 完了**。投稿基盤 + 危険箇所報告（F-2）を実装（`feat/p3-posts`）。
  `GET/POST /api/reports`・`GET/DELETE /api/reports/:id`・`POST /api/reports/:id/comments`・
  `GET /api/photos/:id/:n` と、投稿フォーム S-4・詳細パネル S-3・一覧 S-5・地図のピン。
- 2026-08-24: 投稿の定義（カテゴリ・色・固有項目・上限）を `app/src/lib/reports.ts` の
  `REPORT_CATEGORIES` 1 箇所に集約。フォーム・詳細・一覧・地図が全部そこを読むので、
  **P4（浸水）・P5（観光）はカテゴリを 1 行足すだけ**で入り口が増える。
- 2026-08-24: 詳細パネル用に **`GET /api/reports/:id` を I-5 に追加**（interfaces.md を更新）。
  投稿者の ID は返さず、サーバーが突き合わせた `isAuthor` だけを返して削除ボタンの出し分けに使う。
- 2026-08-24: `compose.yaml` に `uploads` ボリュームを追加し、`Dockerfile` で `/app/uploads` を
  `node` 所有にした。**実機で `USER node` から書けることを確認**（volume は空のときイメージ側の所有者を継ぐ）。
- 2026-08-24: 実測でわかったこと — **写真の置き場を環境変数にすると Next のビルドがパスを追えず、
  「プロジェクト全体を出力に同梱する」警告が 3 件出る**。`path.join(process.cwd(), "uploads", …)` に
  固定して解消。実行イメージが無駄に膨らむのを防ぐ。
- 2026-08-24: 入力の健全性（requirements.md 10-1 の必須 2 項目）を実装。写真は JPEG/PNG/WebP・
  1 枚 5 MB・3 枚・1 リクエスト 10 MB で、**申告 MIME だけでなく先頭バイトも検査**する。
  `city_code`・`is_official`・`rainfallMm` はクライアントから受け取らずサーバーが決める。
- 2026-08-24: **P4 完了**。浸水報告（F-3）と注意案内（F-4）を実装（`feat/p4-flood-weather`）。
  `GET /api/weather`（I-6）で気象庁 JSON を中継し、浸水投稿にサーバーが雨量を焼き込む。
  雨の予報が出ているとき、過去の浸水報告に注意カードと地図の輪を出す。
- 2026-08-24: **F-4（P7）を P4 に前倒し**した。F-3 と同じ気象 API を土台に持ち、分けると
  同じ取得処理を 2 回に分けて書くことになるため。理由は `requirements.md` §9-3 に記録。
- 2026-08-24: 実測でわかったこと — `amedastable.json` の緯度経度は **`[度, 分]` の配列**
  （小数の度ではない）。観測値は **`[数値, 品質フラグ]`** でフラグ 0 以外は欠測。
  **市川市にアメダスが無く最寄りは船橋で約 10 km**なので、観測所名と距離を必ず併記する。
- 2026-08-24: 予報区は**市町村単位では取れない**（千葉県は北西部・北東部・南部の 3 区分）。
  `common/const/area.json` の class20s → class15s → class10s → offices を辿れば
  **全国の市区町村コードから予報区を引ける**ので、DB にカラムを足さず千葉県全域に効く。
- 2026-08-24: 降水確率のしきい値を **30%** と決めた（傘を持つ目安）。上げると雨でも注意が
  出ない回が増え、下げるとほぼ毎日出て意味が薄れる。画面には気象庁の確率をそのまま出す。
- 2026-08-24: F-4 の文言は**気象業務法の線**を守る。「過去に N 件の浸水報告」「気象庁の予報で
  降水確率 最大 X%」の事実 2 つだけを並べ、予報表現は書かない。文言は `requirements.md` §3-1 に固定。
- 2026-08-24: 気象庁への到達を落とした状態（`extra_hosts` で DNS を潰す）で実測 — 浸水投稿は
  201 で成功し、詳細に「投稿時の雨量: 取得できませんでした」と出て、注意案内は出ない。
  クライアントが `rainfallMm` を詐称して送っても、サーバーが捨てて自前の値で上書きすることも実測。
- 2026-08-24: 景観100選 100 件を取得して GeoJSON 化（`feat/p5-tourism`）。データセット 3291・
  CC BY 4.0。`build_geojson.py` に変換を 1 本足し、`app/public/data/scenic_spots.geojson` を出力。
- 2026-08-24: 実測でわかったこと — 景観100選の**カテゴリの区切りは読点「、」**（半角カンマは 0 件）。
  **画像列は 3 通りの URL すべてで 404** なので使わない。解説は日本語 19〜88 字・英語 57〜365 字で全件あり。
- 2026-08-24: **P5 完了**。観光マップ（F-5）と観光おすすめ投稿（F-6）を実装。景観100選を
  日英切替つきポップアップで表示し、既存の徒歩ナビにそのまま載る。`/?mode=tourism` で観光モード。
- 2026-08-24: 設計判断 — 景観スポットは施設レイヤー（`layers.ts`）と**別に持つ**（`scenic.ts`）。
  日英 2 か国語・1 件が複数カテゴリという形が `FacilityProps`／`popupFields` に収まらないため。
  徒歩ナビだけ共通化し、`geo.ts` の最寄り探索を `NavCandidate` ベースに一般化した。
- 2026-08-24: 実測で判明 — **MapLibre は GeoJSON の配列プロパティを JSON 文字列に畳む**。
  地図の `match` 式やフィルタから読む値は、はじめから文字列の別キーで持つ必要がある
  （景観スポットの `categoryPrimary`）。表示側は `scenicCategories()` で両方の形を均す。
- 2026-08-24: モード（S-1 防災 / S-2 観光）を `mapModes.ts` に切り出した。**決めるのは初期値だけ**で、
  切り替え後にレイヤーを足し引きするのは自由。投稿できるカテゴリもモードごとの配列になった。
- 2026-08-27: 追加要件 — モード切替を**ヘッダー直下のタブ**（`MapModeTabs.tsx`）にした。
  操作パネルの中のセグメントだと「いまどちらの地図か」が地図より後に目に入り、伝わらなかった。
- 2026-08-27: 追加要件 — **行政の投稿を一目で分かる**ようにした。一覧と詳細に「行政の公式投稿」
  バッジ、地図では**ピンの輪を公式色**（`authorRole === "gov"`）。観光おすすめは住民と行政の両方が投稿できる。
- 2026-08-27: P4 と P5 の噛み合わせを直した。雨量カードは**浸水を投稿できるモードだけ**、
  注意案内（F-4）は**浸水のピンを出しているときだけ**表示する（地図側の条件と揃えた）。
- 2026-08-27: 表示名を「市川市 オープンデータマップ」から **CHIZUBA** にリブランド
  （`style/chizuba-rebrand`）。ワードマーク・ブラウザタイトル（`title.template`）・
  /about のコンセプト紹介・README・OSRM の User-Agent・npm パッケージ名まで同期。
- 2026-08-27: 実測でわかったこと — **ヘッダー帯にワードマークを足すとスマホ幅で溢れる**。
  386px では「デモモードで動作中」＋投稿一覧＋ログイン状態で既に一杯で、表示名が
  truncate で幅 0 まで潰れ行政バッジがはみ出す。**sm（640px）以上でだけ出す**ことにした。
- 2026-08-27: `compose.yaml` の `name: ichikawa-opendata-map` は**変えない**と決めた。
  変えると起動中のコンテナが別プロジェクト扱いになり作り直しになる。理由は `app/README.md` に明記。
- 2026-08-27: CHIZUBA のロゴを自作（`app/src/app/icon.svg`）。開いた C の弧＋地点の点の 2 要素で、
  16px でも潰れない。`favicon.ico`（16/32/48）と `apple-icon.png`（180px）を `rsvg-convert` で起こし、
  画面の中で使う分は `BrandMark.tsx` に同じ図形を写した。
- 2026-08-27: 実測でわかったこと — **XML のコメントに二重ハイフンは書けない**。
  SVG のコメントに CSS 変数名（`--color-ink`）を書いたら rsvg のパースが落ちた。
- 2026-08-27: 方針 — **このアプリはスマホ前提**。帯（`AuthBar`）を 3 段構えにし、
  375px で余白 72px を確保。320px でも溢れない（要件で縮められない「デモモードで動作中」は
  文言を残し、飾りのアイコンのほうを落として幅を作った）。
- 2026-08-27: デモ投稿 22 件（危険箇所 7・浸水 5・観光 10）を初回起動時に入れるようにした
  （`feat/demo-seed`）。`db/init/003_seed_demo_reports.sql` で投入し、写真 17 枚は
  `db/seed-photos/` に置いて **Dockerfile が `/app/uploads` へ配る**（API もスキーマも変えない）。
- 2026-08-27: デモを実データに見せない建付けを入れた。判定は `details.demo`（`isDemoReport`）で、
  一覧・詳細にバッジ、**浸水の雨量は気象庁出典を出さず「デモ用のダミー値」とだけ表示**する。
  過去の実測値は気象庁 JSON から取れないので、観測値のふりをさせない。
- 2026-08-27: 実測で確認 — `demo: true` を付けて `POST /api/reports` しても、`parseDetails` が
  カテゴリ定義外のキーを捨てるので**利用者はデモバッジを詐称できない**（201 の応答に `demo` が無い）。
- 2026-08-27: デモ投稿の写真はウィキメディア・コモンズの再利用可（CC0 / CC BY / CC BY-SA）のみ。
  **防災の 7 枚は市外の参考写真**で、市川市の被害写真ではない旨を `/about` に明記した。
  出典の正本は `credits.ts` の `DEMO_PHOTO_CREDITS`、取得の記録は `data/wikimedia-commons/SOURCE.md`。
- 2026-08-27: **P6 完了**。行政の対応状況の更新と投稿者による本文編集を実装（`feat/p6-extensions`）。
  `PATCH /api/reports/:id` の 1 経路に 2 つの操作を載せ、**含まれている項目ごとに権限を確かめる**
  （`status` は担当市町村の行政、本文は投稿者本人）。詳細パネル S-3 の中に置いた。
- 2026-08-27: 設計判断 — PATCH の `details` は**丸ごと置き換えず `||` で重ねる**。
  浸水投稿の `rainfallMm` などサーバーが焼き込んだ値が、本文を直すたびに消えるのを防ぐため。
  `rainfallMm` を混ぜて送っても I-4 と同じ検証で捨てられることは実測で確認。
- 2026-08-27: 投稿を**日付の範囲で遡れる**ようにした（浸水実績アーカイブ・`from` / `to`）。
  地図は `DateRangeFilter`、一覧はリンクと GET フォーム（JavaScript 不要）。
  **注意案内 F-4 は絞り込みの影響を受けない**（根拠の浸水報告だけ全期間で引き直す）。
- 2026-08-27: 実測でわかったこと — **`date AT TIME ZONE 'Asia/Tokyo'` は逆向きに解決される**。
  date → timestamptz の暗黙キャストが選ばれ、境目が 9 時間ずれて単日の指定が 1 件も引けない。
  `::timestamp` を明示して解決（PostgreSQL 17 で実測。`reportStore.ts` にコメントを残した）。
- 2026-08-27: デモの浸水投稿 5 件を**3 つの「雨のあった日」に散らした**（約 1 日前 3・4 日前 1・
  9 日前 1）。全部が同じ日に固まっていて、期間で絞る操作を試せなかったため。10 日以内の制約は維持。
- 2026-08-27: 投稿を **CSV / GeoJSON で書き出せる**ようにした（I-10）。絞り込みの条件をそのまま
  受け取る。CSV は UTF-8 BOM + CRLF + RFC 4180、GeoJSON は RFC 7946（公式 JSON Schema でエラー 0）。
  **デモ投稿の雨量は書き出さない**（観測値ではないので観測値の列に入れない）。
- 2026-08-27: CSV は**利用者が書いた文字列が `=` `+` `-` `@` で始まるとき先頭に `'` を足す**。
  表計算ソフトが本文を数式として実行するのを止めるため（中身は変えない）。
- 2026-08-27: キーワード検索を追加。**両側を NFKC + 小文字にそろえる**ので「ｱﾝﾀﾞｰﾊﾟｽ」でも当たる
  （PostgreSQL の `normalize()` は 13 以降の組み込み）。LIKE のワイルドカードは打ち消す。
  景観スポットは 100 件しかないので画面側で探す。現在地ボタンは MapLibre の GeolocateControl。
- 2026-08-27: **セッションの穴を塞いだ**。別 DB で発行した行政トークンが 200 で通り、
  `uid` がその DB に存在しなくても行政操作ができていた（Cookie はポートを区別しないので
  同じ localhost の別インスタンスでも起きる）。`app_instance.install_id` で
  **署名鍵を導き、トークンにも焼き込んで毎回突き合わせる**二重の縛りに変更。
- 2026-08-27: 方針を恒久記載 — **モバイルファースト**（`.agent/conventions.md`）。
  新しい UI は 375px 幅で先に決めてから広げ、**375px で実際に動かして確かめる**。
- 2026-08-27: **モバイル UI 総点検**（`style/mobile-polish`）。375px/320px/414px で全フローを
  実操作し、破綻 9 件を修正。最大は「初回表示で地図が 119px しか見えない」（→ 514px）と
  「ピンを押してもポップアップがシートの裏で見えない」（z-index）。
- 2026-08-27: 実測でわかったこと — **MapLibre のポップアップは z-index を持たず、
  入りきらなくても縮めない**。重ねる UI がある画面では自分で `z-index` と高さの上限
  （地図の高さの半分＝スマホで 40dvh）を決める必要がある。恒久メモは `.agent/conventions.md`。
- 2026-08-27: 実測でわかったこと — **`input[type="range"]` は要素の高さがそのまま
  指の当たり判定**。見た目に合わせて `h-1` を当てると 4px の帯になる。
  320px では投稿詳細のヘッダーが折り返せず、閉じるボタンが 45px 画面外に出ていた。

- 2026-08-27: 公開運用向けに **`GOV_DEMO_PIN`** を追加（`feat/gov-pin-headless`）。
  設定した環境だけ、デモログインの行政ロールに PIN を要求する。**未設定なら従来どおり**
  （審査員の体験は変えない）。総当たり対策は失敗ごとの待機（最大 8 秒）＋連続 10 回で 60 秒。
- 2026-08-27: 実測でわかったこと — **React 19 はフォームアクションのあとフォームを reset する**。
  入力欄の出し入れを `useState` でやると「ラジオは戻ったのに欄は出たまま」になり、
  そのまま送ると値が化ける。**`peer-has-checked` で CSS から出す**と reset と必ず一致する。
- 2026-08-27: `setup.sh` を colima + Homebrew でも通るようにした。`docker compose`
  （プラグイン）が無ければ単体の `docker-compose` へ落ち、tailscale は Homebrew の
  置き場も直接探す。**colima の実機は未検証**（モックで前提チェックまで確認）。
