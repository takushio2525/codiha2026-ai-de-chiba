# 作業履歴

> **書き方**: 作業が一段落したら**追記**する（上書きしない）。
> 1 エントリ **1〜3 行**に収める。詳細は git log とコミットメッセージに任せる。
> 単発の質問やタイポ修正では更新しない。
>
> 古いエントリが 30 件を超えたら `progress-archive.md` に移す。

---

- 2026-08-27: デモを実データに見せない建付けを入れた。判定は `details.demo`（`isDemoReport`）で、
  一覧・詳細にバッジ、**浸水の雨量は気象庁出典を出さず「デモ用のダミー値」とだけ表示**する。
  過去の実測値は気象庁 JSON から取れないので、観測値のふりをさせない。
- 2026-08-27: 実測で確認 — `demo: true` を付けて `POST /api/reports` しても、`parseDetails` が
  カテゴリ定義外のキーを捨てるので**利用者はデモバッジを詐称できない**（201 の応答に `demo` が無い）。
- 2026-08-27: デモ投稿の写真はウィキメディア・コモンズの再利用可（CC0 / CC BY / CC BY-SA）のみ。
  **防災の 7 枚は市外の参考写真**で、市川市の被害写真ではない旨を `/about` に明記した。
  出典の正本は `credits.ts` の `DEMO_PHOTO_CREDITS`、取得の記録は `data/wikimedia-commons/SOURCE.md`。
- 2026-08-27: **P6 完了**。行政の対応状況の更新と投稿者による本文編集を実装（`feat/p6-extensions`）。
  `PATCH /api/reports/:id` の 1 経路に 2 つの操作を載せ、**含まれている項目ごとに権限を確かめる**
  （`status` は担当市町村の行政、本文は投稿者本人）。詳細パネル S-3 の中に置いた。
- 2026-08-27: 設計判断 — PATCH の `details` は**丸ごと置き換えず `||` で重ねる**。
  浸水投稿の `rainfallMm` などサーバーが焼き込んだ値が、本文を直すたびに消えるのを防ぐため。
  `rainfallMm` を混ぜて送っても I-4 と同じ検証で捨てられることは実測で確認。
- 2026-08-27: 投稿を**日付の範囲で遡れる**ようにした（浸水実績アーカイブ・`from` / `to`）。
  地図は `DateRangeFilter`、一覧はリンクと GET フォーム（JavaScript 不要）。
  **注意案内 F-4 は絞り込みの影響を受けない**（根拠の浸水報告だけ全期間で引き直す）。
- 2026-08-27: 実測でわかったこと — **`date AT TIME ZONE 'Asia/Tokyo'` は逆向きに解決される**。
  date → timestamptz の暗黙キャストが選ばれ、境目が 9 時間ずれて単日の指定が 1 件も引けない。
  `::timestamp` を明示して解決（PostgreSQL 17 で実測。`reportStore.ts` にコメントを残した）。
- 2026-08-27: デモの浸水投稿 5 件を**3 つの「雨のあった日」に散らした**（約 1 日前 3・4 日前 1・
  9 日前 1）。全部が同じ日に固まっていて、期間で絞る操作を試せなかったため。10 日以内の制約は維持。
- 2026-08-27: 投稿を **CSV / GeoJSON で書き出せる**ようにした（I-10）。絞り込みの条件をそのまま
  受け取る。CSV は UTF-8 BOM + CRLF + RFC 4180、GeoJSON は RFC 7946（公式 JSON Schema でエラー 0）。
  **デモ投稿の雨量は書き出さない**（観測値ではないので観測値の列に入れない）。
- 2026-08-27: CSV は**利用者が書いた文字列が `=` `+` `-` `@` で始まるとき先頭に `'` を足す**。
  表計算ソフトが本文を数式として実行するのを止めるため（中身は変えない）。
- 2026-08-27: キーワード検索を追加。**両側を NFKC + 小文字にそろえる**ので「ｱﾝﾀﾞｰﾊﾟｽ」でも当たる
  （PostgreSQL の `normalize()` は 13 以降の組み込み）。LIKE のワイルドカードは打ち消す。
  景観スポットは 100 件しかないので画面側で探す。現在地ボタンは MapLibre の GeolocateControl。
- 2026-08-27: **セッションの穴を塞いだ**。別 DB で発行した行政トークンが 200 で通り、
  `uid` がその DB に存在しなくても行政操作ができていた（Cookie はポートを区別しないので
  同じ localhost の別インスタンスでも起きる）。`app_instance.install_id` で
  **署名鍵を導き、トークンにも焼き込んで毎回突き合わせる**二重の縛りに変更。
- 2026-08-27: 方針を恒久記載 — **モバイルファースト**（`.agent/conventions.md`）。
  新しい UI は 375px 幅で先に決めてから広げ、**375px で実際に動かして確かめる**。
- 2026-08-27: **モバイル UI 総点検**（`style/mobile-polish`）。375px/320px/414px で全フローを
  実操作し、破綻 9 件を修正。最大は「初回表示で地図が 119px しか見えない」（→ 514px）と
  「ピンを押してもポップアップがシートの裏で見えない」（z-index）。
- 2026-08-27: 実測でわかったこと — **MapLibre のポップアップは z-index を持たず、
  入りきらなくても縮めない**。重ねる UI がある画面では自分で `z-index` と高さの上限
  （地図の高さの半分＝スマホで 40dvh）を決める必要がある。恒久メモは `.agent/conventions.md`。
- 2026-08-27: 実測でわかったこと — **`input[type="range"]` は要素の高さがそのまま
  指の当たり判定**。見た目に合わせて `h-1` を当てると 4px の帯になる。
  320px では投稿詳細のヘッダーが折り返せず、閉じるボタンが 45px 画面外に出ていた。
- 2026-08-27: 公開運用向けに **`GOV_DEMO_PIN`** を追加（`feat/gov-pin-headless`）。
  設定した環境だけ、デモログインの行政ロールに PIN を要求する。**未設定なら従来どおり**
  （審査員の体験は変えない）。総当たり対策は失敗ごとの待機（最大 8 秒）＋連続 10 回で 60 秒。
- 2026-08-27: 実測でわかったこと — **React 19 はフォームアクションのあとフォームを reset する**。
  入力欄の出し入れを `useState` でやると「ラジオは戻ったのに欄は出たまま」になり、
  そのまま送ると値が化ける。**`peer-has-checked` で CSS から出す**と reset と必ず一致する。
- 2026-08-27: `setup.sh` を colima + Homebrew でも通るようにした。`docker compose`
  （プラグイン）が無ければ単体の `docker-compose` へ落ち、tailscale は Homebrew の
  置き場も直接探す。**colima の実機は未検証**（モックで前提チェックまで確認）。
- 2026-08-27: 景観100選のスポット写真をコモンズから集め、**100 か所中 54 か所**に付けた
  （`feat/spot-photos`）。ポップアップに写真＋作者＋ライセンス、全 54 枚の一覧は `/about`。
  取得と生成は `data/scripts/fetch_scenic_photos.py` → `build_scenic_photos_ts.py`。
- 2026-08-27: 実測でわかったこと — **スポット名で検索すると同名別所が大量に混ざる**。
  愛知県日進市の弁天池公園・岐阜県北方町の北方小学校・郡山市立行徳小学校・静岡の徳願寺・
  宗谷岬（曽谷）・春日部市の八幡神社など。**geosearch か専用カテゴリで裏を取り、最後に目視**する。
- 2026-08-27: 実測でわかったこと — コモンズの `imageinfo.thumburl` は**元が指定幅より小さいと
  原寸 URL を返す**。そのまま落とすと 1 枚 10 MB・12 秒かかった。
  `Special:FilePath/<名前>?width=N` なら必ず縮小版が返る。
- 2026-08-27: **ログインのリダイレクトが常に `localhost:3000` へ飛ぶ不具合を修正**（`fix/auth-redirect-host`）。
  `compose.yaml` の `AUTH_URL` 固定をやめ、`X-Forwarded-Host` →（無ければ）`Host` から毎回導くようにした。
  設計判断は `.agent/architecture.md`「公開 URL の決め方」、実測は `requirements.md` §8-7。
- 2026-08-27: 実測でわかったこと — **`AUTH_URL` を外すだけでは直らない**。`output: "standalone"` の
  Next.js は待ち受けアドレス（`HOSTNAME=0.0.0.0`）からリクエスト URL を組むので、`/api/auth/*` の
  ルートハンドラに `http://0.0.0.0:3000/...` が渡り、リダイレクトが `0.0.0.0` になる。
  ルートハンドラ側で URL を組み直す必要がある（`src/lib/publicOrigin.ts`）。
- 2026-08-27: 実測でわかったこと — Auth.js は経路によって URL の決め方が違う。**サーバーアクション**
  （`signIn`/`signOut`）は `createActionURL()` がヘッダーを見るので `trustHost: true` だけで足り、
  **ルートハンドラ**は `Auth()` が `req.url` を見るので自分で直す。片方だけ直すと気づきにくい。
- 2026-08-27: 公開ポートを `${CHIZUBA_PORT:-3000}` に変数化（既定 3000 は据え置き）。
  `setup.sh --port N` は一時オーバーレイをやめて `CHIZUBA_PORT=N` を渡すだけになり、
  `AUTH_URL` の書き込みもやめた（古い値が残ると自動判定より優先されて壊れるので警告に変更）。
- 2026-08-27: 実測でわかったこと — 壊れた `X-Forwarded-Host`（`evil.test/path` など）を弾いたあと
  `Host` に落とさないと `0.0.0.0` に戻る。候補を順に試して**最初に形が通ったもの**を採る作りにした。
- 2026-08-27: **app/src の横断リファクタ**（`refactor/consolidation`）。9 フェーズの積み上げで
  生じた重複を統合し、デッドコードを除去した（挙動変更ゼロ）。何をなぜ直したかは PR と git log。
- 2026-08-27: 実測でわかったこと — **Tailwind v4 の任意値（`bg-[#0072b2]`）は class に直書きしないと
  拾われない**ので JS 定数と共有できない。地図の式（`MapView` の `OFFICIAL_COLOR`）とバッジの色は
  「同じ値を別々に書く」しか揃える手が無く、コメントで縛るしかない。
- 2026-08-27: リファクタの回帰確認は**レンダリング後の DOM を before/after で丸ごと突き合わせる**のが
  速くて確実だった（class 文字列まで一致を見られる）。API は 18 経路のスナップショットを取り、
  時刻（`generatedAt`）だけ除いて差分ゼロを確認した。
- 2026-08-30: **自宅の Mac（`oldmac`）へセルフホストして Tailscale Funnel で公開した**。
  外部 ingress 経由で HTTP 200・Let's Encrypt 証明書・デモログイン成立まで実測。
  **リポ変更ゼロ**（`setup.sh` をそのまま使用）。残件は再起動復帰の 2 点（下記）。
- 2026-08-30: 実測でわかったこと — **`pmset` の `sleep 0` はアイドルスリープしか止めない**。
  画面スリープ→`darkwakelinger` でダークウェイクに入ると Tailscale ごと 8 分間 offline になった。
  `setup.sh` のスリープ判定は `sleep` しか見ないので、この状態を「OK」と誤判定する。
  恒久対策は `sudo pmset -a sleep 0 disablesleep 1 displaysleep 0`（sudo が要る）。
- 2026-08-30: 実測でわかったこと — **非対話 SSH の macOS の PATH に `/usr/local/bin` は無い**。
  Docker Desktop を入れてあっても `setup.sh` が「docker が見つかりません」で止まる。
  tailscale と同じくパス候補を直接並べる必要がある。
- 2026-08-30: 実測でわかったこと — **Funnel の URL は MagicDNS 配下では外部検証にならない**。
  tailnet 内から引くと 100.x が返るので、公開 DNS が返す ingress（103.84.155.x）を
  `curl --resolve` で強制しないと「Tailscale 無しで開けるか」を確かめたことにならない。
- 2026-08-30: **Google ログインとデモログインを併存**させた（`feat/google-demo-coexist`）。
  `AUTH_MODE` の排他をやめ、鍵の有無は「Google を足すか」だけに効かせる。
  **鍵が無い環境の見える DOM は 1 文（事実と食い違う案内文）以外そのまま**なのを
  main とのレンダリング差分で確認した。
- 2026-08-30: `setup.sh` の前提チェックを実機で踏んだ不備 4 件ぶん直した
  （`fix/setup-headless-detection`）。docker のパス探索・tailscale CLI の版合わせ・
  スリープ判定の偽陽性・**bash 3.2 で `$VAR` の直後に全角文字を置くと
  変数名に食われる**（既存 5 箇所も `${VAR}` に修正）。
- 2026-08-30: 実測でわかったこと — **macOS 標準の bash 3.2 は `set -u` のもとで
  空配列の `"${ARR[@]}"` が unbound variable で落ちる**。`${ARR[@]+"${ARR[@]}"}` が要る。
  母艦の bash 5 では再現しないので、`bash -n` だけでは見つからない。

- 2026-08-31: **プライバシーポリシー `/privacy` を追加**（S-8・PR #24 / #25）。Google OAuth の
  本番公開に同じドメインの URL が要るため。**中身は実装を読んで書いた**ので、挙動を変えたら
  このページも直す（根拠の対応表はページ冒頭のコメント）。入口はフッター・`/about`・`/login`。
- 2026-08-31: 実装を読んで分かった、正直に書くべきこと 3 つ — **暗号化セッション Cookie の中には
  Google のメールと画像 URL が残る**（`@auth/core` の既定トークン）／**写真は受け取ったバイト列を
  そのまま保存するので Exif が消えない**（`photoStore.ts`）／**アカウントを利用者自身で消す画面が無い**。
- 2026-08-31: 実測でわかったこと — **JSX はソースの改行を半角空白 1 つに変える**ので、
  日本語を語の途中で折り返すと文中に空白が見える。**句読点の直後で折り返す**と消える
  （`/about` にも同じ空白が残っている。害は小さいので既存は触っていない）。
- 2026-08-31: **やらかしと復旧** — oldmac への反映で素の `docker compose up -d` を打ち、
  `compose.prod.yaml` 無しでコンテナが作り直された。**`restart` が `no` に戻り、公開ポートが
  `0.0.0.0` に開いた**（LAN から素の HTTP で入れる状態）。`-f` を 2 枚重ねて打ち直して復旧し、
  `deploy/selfhost/README.md` に警告を足した。データはボリュームなので無事（投稿 23 件を確認）。
