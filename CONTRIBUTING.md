# コントリビューションガイド

このリポジトリで開発するときのルール集。**Git も共同開発もほぼ初めて**という
メンバーを想定して書いている。慣れている人は「ブランチ戦略」以降を斜め読みでOK。

> このガイドでは **VS Code（GitLens）** と **ターミナル** の2通りで手順を説明している。
> 好きな方を使えばよいが、慣れないうちは **VS Code の GUI** が分かりやすい。
>
> - [VS Code で操作する場合 →](#3-vs-code-で操作する場合の手順)
> - [ターミナルで操作する場合 →](#4-ターミナルで操作する場合の手順)

---

## 🚨 一番大事なルール：作業前に必ずリポジトリを最新化する

コード編集に取りかかる**前に**、ローカルのリポジトリを最新の状態に揃える。

- **VS Code**: 左下のブランチ名クリック → `main` 選択 → ステータスバーの **同期アイコン（🔄）** をクリック
- **ターミナル**: `git switch main && git pull`

**忘れるとどうなるか**：

- Bさんが昨日 main に PR をマージした
- あなたはそれに気づかずに、古い main からブランチを切って作業
- PR を出すときに「謎のコンフリクト」が大量発生して解消に1時間…

チームメンバーが push した変更を取り込まないまま作業を始めると、
PR を出すときにコンフリクトが大量発生して時間を浪費する。
「作業の最初に必ず最新化」を徹底すること。

---

## 🚨 もう一つ大事なルール：このリポジトリは「public 前提」で運用する

授業では最後に成果物を教員・TA に見てもらう。private のままだと閲覧してもらえず、
提出直前に慌てて public 化することになる。そのときに個人情報や授業資料が
混ざっていると、**履歴に残るので簡単には消せない**。

なので**最初から「いつ public になっても平気」な状態で運用する**。

- 個人レポート・授業資料・評価物は、このリポジトリではなく**別の private リポジトリ**へ
- 迷ったときの判断ルール:
  **氏名・学籍番号・著作権物・評価情報が 1 つでも入るなら private。5 秒迷ったら private。**

線引き表と詳しい理由は [`docs/before_coding.md`](docs/before_coding.md) にある。
運用の具体手順は [9. リポジトリに入れてよいもの・ダメなもの](#9-リポジトリに入れてよいものダメなもの) を参照。

---

## 1. 最初のセットアップ（Git を初めて使う人向け）

### 1.1 Git のインストール

- macOS: `xcode-select --install` か Homebrew で `brew install git`
- Windows: [Git for Windows](https://gitforwindows.org/) をインストール
- Linux: `sudo apt install git` など

インストールできたか確認：

```bash
git --version
```

### 1.2 VS Code と推奨拡張機能のインストール

このリポジトリでは **VS Code** をエディタとして推奨している。

1. [VS Code 公式サイト](https://code.visualstudio.com/) からインストール
2. VS Code でこのリポジトリのフォルダを開く
3. 右下に「**推奨拡張機能をインストールしますか？**」と通知が出るので、**すべてインストール** を選択

手動で入れる場合は、サイドバーの「拡張機能」アイコン（四角が4つ）を開き、以下を検索してインストール：

| 拡張機能 | 用途 |
|---|---|
| **GitLens** (`eamodio.gitlens`) | Git の履歴・差分・ブランチ管理を GUI で操作 |
| **EditorConfig** | コーディングスタイルの自動統一 |
| **PlatformIO IDE** | Arduino 開発する班向け |
| **LaTeX Workshop** | 報告書（LaTeX）を書く人向け |

### 1.3 名前とメールアドレスを登録（初回のみ）

commit の作者情報として使われる。GitHub に登録したアドレスを使うと、
GitHub 上であなたのアイコンが表示されて分かりやすい。

VS Code 内のターミナル（`` Ctrl+` `` で開く）か、普通のターミナルで以下を実行：

```bash
git config --global user.name "Your Name"
git config --global user.email "you@example.com"
```

> **⚠️ 大学から配布された学内メールアドレスは使わない。**
>
> ここで設定したアドレスは**全コミットに刻まれ、public リポジトリでは誰でも読める**。
> 学内メールは学籍番号がそのままアドレスになっていることが多く、
> 一度 push すると履歴から消すのが非常に面倒になる。
>
> 個人の Gmail か、GitHub が用意している公開用アドレス（`12345678+username@users.noreply.github.com`。
> GitHub の Settings → Emails で確認できる）を使う。
>
> 設定を間違えたまま数コミットしてしまった場合は、**push する前なら**やり直せる。
> push 済みなら、まずチームに共有してから対応を相談する。

### 1.4 GitHub との認証

HTTPS で clone するなら、push のときに GitHub の**個人アクセストークン（PAT）**を
求められる。パスワードは使えないので注意。

- 参考: [GitHub: 個人アクセストークンの作成](https://docs.github.com/ja/authentication/keeping-your-account-and-data-secure/managing-your-personal-access-tokens)

より楽なのは **SSH 鍵** を登録する方法。一度設定すればパスワード不要になる。

- 参考: [GitHub: SSHでの接続](https://docs.github.com/ja/authentication/connecting-to-github-with-ssh)

### 1.5 Git の全体像：ローカルとクラウドの関係

操作を覚える前に、**「どこからどこへデータが動いているのか」**を掴んでおくと迷わない。

```
   あなたのPC                                    クラウド（GitHub）
 ┌───────────────────────────────────┐        ┌──────────────────┐
 │  作業フォルダ                     │        │                  │
 │      │ git add                    │        │   リモート        │
 │      ▼                            │        │   リポジトリ      │
 │  ステージ（コミット予定の置き場） │        │   （origin）      │
 │      │ git commit                 │        │                  │
 │      ▼                            │ push   │                  │
 │  ローカルリポジトリ ──────────────┼───────▶│                  │
 │  （履歴が入っている）             │        │                  │
 │                     ◀─────────────┼────────│                  │
 └───────────────────────────────────┘  pull  └──────────────────┘
```

| 操作 | 何をするか | ひとこと |
|---|---|---|
| `git add` | 変更を「コミットする候補」に入れる | まだ履歴には入らない |
| `git commit` | 候補をまとめて**ローカルの履歴**に刻む | まだ GitHub には無い |
| `git push` | ローカルの履歴を**クラウドへ送る** | ここで初めて他の人に見える |
| `git pull` | クラウドの履歴を**手元に取り込む** | 実体は fetch + merge |
| `git merge` | 別のブランチの変更を今のブランチに合流させる | コンフリクトが起きうるのはここ |

**よくある誤解**

- ❌「commit したから他の人にも見えている」→ push するまで自分の PC の中だけ
- ❌「push すれば main に入る」→ 入るのは自分のブランチ。main に入るのは PR がマージされたとき
- ❌「pull はダウンロードするだけ」→ 手元の変更と**合流（merge）**する。だから衝突しうる

### 1.6 このチームの Git ルール（2 つだけ）

**① `rebase` は使わない**

`git rebase` は履歴を書き換える操作で、うまく使えば履歴がきれいになる。
ただし**書き換えた履歴を push すると、他の人の手元と食い違って事故になる**。
初めての共同開発では利点より事故のほうが大きいので、このチームでは使わない。

main の変更を取り込むときは `merge` を使う。履歴に merge commit が残るが、それでよい。

```bash
# 作業ブランチに main を取り込む（rebase ではなく merge）
git switch feature/my-work
git fetch origin
git merge origin/main
```

**② main の取り込みは「1 日 1 回」**

作業ブランチを長く持つほど、main との差が開いてコンフリクトが大きくなる。
**作業を始めるときに 1 回、main を取り込む**のを習慣にする。

小さいコンフリクトを毎日 5 分ずつ潰すほうが、1 週間ぶんを一気に潰すより圧倒的に楽。

---

## 2. ブランチ戦略

### 基本ルール

- `main` ブランチへ**直接 push は禁止**
- 機能追加・バグ修正は必ずブランチを切って作業する
- 作業が終わったら Pull Request（PR）を作って、レビューしてからマージ

### ブランチ名の付け方

種類 / 短い説明 の形式：

| 種類 | 例 |
|---|---|
| `feature/` | `feature/add-sensor-reading` |
| `fix/` | `fix/null-pointer-on-startup` |
| `docs/` | `docs/update-readme` |
| `refactor/` | `refactor/split-comm-module` |

- 英語でも日本語でもよいが、短く・内容が分かる名前にする
- スペースは使わない（`-` か `_` でつなぐ）

### ブランチ切りのコツ

1. **必ず最新の main から切る**（pull してからブランチを作る）
2. **1ブランチ = 1目的**（レビュー負担を減らす）
3. **長期間持たない**：1週間以上持つとコンフリクト地獄になる。大きい作業は
   途中で PR を分ける
4. **作業中に main が進んだら取り込む**（main の変更を自分のブランチに merge）
5. **マージされたブランチは消す**

---

## 3. VS Code で操作する場合の手順

VS Code + GitLens を使った、clone から PR までの一連の流れ。

### 3.1 リポジトリを clone する

1. コマンドパレット（`Ctrl+Shift+P` / macOS: `Cmd+Shift+P`）を開く
2. `Git: Clone` と入力して選択
3. リポジトリの URL（`https://github.com/<組織名>/<リポジトリ名>.git`）を貼り付けて Enter
4. 保存先フォルダを選ぶ → clone が始まる
5. 「クローンしたリポジトリを開きますか？」と聞かれるので **「開く」** を選択

### 3.2 最新化してブランチを作る

1. 左下のブランチ名（例: `main`）をクリック → `main` に切り替え
2. ステータスバーの **同期アイコン（🔄 循環矢印）** をクリックして pull
3. 再び左下のブランチ名をクリック → 「**+ 新しいブランチの作成...**」を選択
4. ブランチ名（例: `feature/my-task`）を入力して Enter

> **注意**: 新しいブランチを作る前に**必ず** main に切り替えて同期（pull）すること。
> これを忘れると、古い状態からブランチが生えてしまい、後でコンフリクトの原因になる。

### 3.3 ファイルを編集する

VS Code でファイルを開いて編集する。

### 3.4 変更を確認する

1. サイドバーの「**ソース管理**」アイコン（`Ctrl+Shift+G` / macOS: `Cmd+Shift+G`）をクリック
2. 「変更」セクションに変更されたファイルの一覧が表示される
3. ファイル名をクリックすると**差分（diff）** が表示される（左が変更前、右が変更後）

> GitLens を入れていると、エディタ上で各行の最終変更者・日時がインライン表示される。

### 3.5 ステージングする（コミットするファイルを選ぶ）

1. 「ソース管理」パネルで、ステージしたいファイルの右側にある **「+」ボタン** をクリック
2. クリックしたファイルが「**ステージされている変更**」セクションに移動する
3. 間違えたら「ステージされている変更」のファイル右側の **「−」ボタン** で戻せる

> 全ファイルを一括ステージしたい場合は「変更」セクションのタイトル右側の「+」を押す。
> ただし `.env` 等を含めないよう注意。

### 3.6 コミットする

1. 「ソース管理」パネル上部の**メッセージ入力欄**にコミットメッセージを入力
   - 例: `[機能追加] センサ読み取り処理を追加`
   - 書式は [コミットメッセージ規約](#5-コミットメッセージ規約) を参照
2. **✓（チェックマーク）ボタン** をクリック、または `Ctrl+Enter`（macOS: `Cmd+Enter`）

### 3.7 push する（GitHub にアップロード）

1. 「ソース管理」パネルの **「…」メニュー** → **「Push」** を選択
2. 初回は「リモートブランチがありません。公開しますか？」と聞かれるので **「OK」** を選択
3. または、ステータスバーの **↑ アイコン（雲マーク）** をクリックしても push できる

### 3.8 Pull Request を作る

1. GitHub のリポジトリページをブラウザで開く
2. 黄色いバナー「Compare & pull request」が出ているのでクリック
3. PR テンプレートが自動で入るので、埋めて「Create pull request」
4. レビュアーを指定する

> VS Code で **GitHub Pull Requests** 拡張（`GitHub.vscode-pull-request-github`）を入れると、
> VS Code 内から PR の作成・レビューもできる。

### 3.9 レビュー指摘に対応する

1. 同じブランチのまま、ファイルを編集 → ステージ（+ボタン） → コミット（✓ボタン） → push
2. PR のページが自動で更新される（GitHub 上でコメントに「Resolve conversation」）

### 3.10 マージ後の片付け

1. GitHub 上でマージボタンを押す
2. 左下のブランチ名クリック → `main` に切り替え
3. 同期アイコン（🔄）をクリックして pull
4. 不要なブランチを消す：コマンドパレット → `Git: Delete Branch...` → 削除するブランチを選択

### 3.11 作業中に main が進んだら

1. コマンドパレット（`Ctrl+Shift+P`）→ `Git: Fetch` で最新情報を取得
2. コマンドパレット → `Git: Merge Branch...` → `origin/main` を選択
3. コンフリクトが起きたら → [コンフリクトの解消（VS Code）](#312-コンフリクトの解消vs-code)

### 3.12 コンフリクトの解消（VS Code）

merge したときに「conflict（衝突）」が起きることがある。2人が同じ行を別々に編集したときに発生する。

1. コンフリクトが発生すると、「**ソース管理**」パネルの「**マージの変更**」セクションに対象ファイルが表示される
2. ファイルを開くと、該当箇所に色付きのハイライトとボタンが表示される：
   - **「現在の変更を取り込む（Accept Current Change）」** — 自分の変更を採用
   - **「入力側の変更を取り込む（Accept Incoming Change）」** — 相手（main）の変更を採用
   - **「両方の変更を取り込む（Accept Both Changes）」** — 両方残す
   - **「変更の比較（Compare Changes）」** — 差分を横並びで確認
3. 正しい内容に編集できたら、ファイルを保存
4. 「ソース管理」パネルで解消済みファイルの **「+」ボタン** でステージ
5. コミットメッセージを入力して ✓ ボタンでコミット

> **ヒント**: どちらの変更が正しいか分からないときは、GitLens の「File History」で
> そのファイルの変更履歴を確認すると判断しやすい。

**一人で悩まず、相手に聞く**のが一番早い。

---

## 4. ターミナルで操作する場合の手順

ターミナル（コマンドライン）を使った、clone から PR までの一連の流れ。
VS Code 内のターミナル（`` Ctrl+` ``）でも、OS のターミナルアプリでもどちらでもよい。

### 4.1 リポジトリを clone する

```bash
# 好きな場所に移動して
cd ~/Documents

# clone
git clone https://github.com/<組織名>/<リポジトリ名>.git
cd <リポジトリ名>
```

### 4.2 最新化してブランチを作る

```bash
git switch main              # main ブランチに移動
git pull                     # リモートから最新を取り込む
git switch -c feature/my-task   # 新しいブランチを作成して切り替え
```

### 4.3 ファイルを編集する

好きなエディタで編集。

### 4.4 変更を確認する

```bash
git status       # どのファイルを変更したか
git diff         # 具体的にどう変わったか
```

### 4.5 ステージングする（コミットするファイルを選ぶ）

```bash
git add path/to/file.c            # 特定のファイル
git add path/to/dir/              # ディレクトリ丸ごと
# git add -A や git add . は便利だが、意図しないファイル（.env 等）まで
# 含めがちなので、慣れるまではファイル名を直接指定するのが安全
```

### 4.6 コミットする

```bash
git commit -m "[機能追加] センサ読み取り処理を追加"
```

コミットメッセージの書式は [コミットメッセージ規約](#5-コミットメッセージ規約) を参照。

### 4.7 push する（GitHub にアップロード）

```bash
# 初回（ブランチを GitHub に作るとき）
git push -u origin feature/my-task

# 2回目以降
git push
```

### 4.8 Pull Request を作る

1. GitHub のリポジトリページをブラウザで開く
2. 黄色いバナー「Compare & pull request」が出ているのでクリック
3. PR テンプレートが自動で入るので、埋めて「Create pull request」
4. レビュアーを指定する

### 4.9 レビュー指摘に対応する

1. 同じブランチで編集 → `git add` → `git commit` → `git push`
2. PR のページが自動で更新される（GitHub 上でコメントに「Resolve conversation」）

### 4.10 マージ後の片付け

```bash
git switch main
git pull
git branch -d feature/my-task   # ローカルのブランチを削除
```

### 4.11 作業中に main が進んだら

```bash
git fetch origin          # 最新情報を取得（ローカルは変わらない）
git merge origin/main     # 自分のブランチに main を取り込む
```

### 4.12 コンフリクトの解消（ターミナル）

merge したときに「conflict（衝突）」が起きることがある。2人が同じ行を別々に編集したときに発生する。

```bash
# merge 後に CONFLICT と出たら

# 1. 対象ファイルを開くと、以下のようなマーカーが入っている
#    <<<<<<< HEAD
#    あなたの変更
#    =======
#    相手の変更
#    >>>>>>> origin/main

# 2. 正しい内容になるよう編集（マーカーは消す）

# 3. 直したファイルを add して commit
git add path/to/conflicted-file
git commit
```

**一人で悩まず、相手に聞く**のが一番早い。

---

## 5. コミットメッセージ規約

`[種別] 変更内容の概要` の形式で書く。

| 種別 | 用途 |
|------|------|
| `[機能追加]` | 新機能の実装 |
| `[修正]` | バグ修正 |
| `[改善]` | 既存機能の改善・パラメータ調整など |
| `[リファクタ]` | コード整理（機能変更なし） |
| `[ドキュメント]` | ドキュメントのみの変更 |
| `[スタイル]` | 見た目・UI の変更 |

### コツ

- 機能単位で**細かく分割**する（無関係な変更を1コミットにまとめない）
- コードの変更と、そのコードに対応する README 更新は**同じコミット**に入れる
- 「なぜ」を意識する（「追加した」ではなく「〇〇のために追加した」の視点）

---

## 6. レビュー観点

PR のレビュー時は以下をチェックする。

### コード全般
- 命名規則に従っているか
- 不要なコメントアウト・`print` / `Serial.println` のデバッグ出力が残っていないか
- エラーケースが考慮されているか

### ドキュメント
- README に書いてある挙動と実装が一致しているか
- 設計判断が大きい変更の場合、`docs/decisions/` に ADR を追加したか

### CI
- GitHub Actions が全部成功しているか（❌ のままマージしない）
- **Secret Scan が緑か**（個人情報・秘匿情報の混入検知。落ちたら中身を必ず確認する）
- PlatformIO Build が緑か（Arduino を使う班のみ。`firmware/` を変更したときに走る）

> CI が赤いまま「あとで直す」でマージすると、次の人の PR も赤いところから始まって
> 誰も直さなくなる。**赤くしたら、赤くした人がその日のうちに直す。**

---

## 7. よくあるミスと対処

### ❌ main に直接 commit してしまった

まだ push していないなら：

```bash
git switch -c feature/my-task   # 今のコミットをブランチに移す
git switch main
git reset --hard origin/main    # main をリモート状態に戻す
git switch feature/my-task
```

push してしまった場合は、リーダーに相談。

### ❌ ステージに意図しないファイルを混ぜた

- **VS Code**: 「ソース管理」パネルの「ステージされている変更」で、外したいファイルの **「−」ボタン** をクリック
- **ターミナル**: `git reset path/to/file`（ステージから外す。中身は消えない）

### ❌ コミットメッセージをタイポした

まだ push していない直前のコミットなら：

```bash
git commit --amend -m "[機能追加] 正しいメッセージ"
```

push した後は、PR の説明欄でフォローするのが安全。

### ⚠️ `git push --force` はやらない

他人のコミットを消してしまう事故のもと。**必要になったら必ずリーダーに相談**。

### 🔥 いちばん怖いミス：「つながっていない 2 つの履歴」が並走する

これは滅多に起きないが、**起きると復旧に半日かかる**。起きうる状況を知っておく。

**どうやって起きるか**

- GitHub 上で先にリポジトリを作り（README 付きで初期化）、
  手元でも `git init` して別々にコミットを始めてしまった
- リモートを消して作り直した / 別のリポジトリを origin に付け替えた
- 誰かが `push --force` で履歴を作り直した

こうなると、**同じファイルを持つのに共通の祖先が無い 2 本の履歴**ができる。
普通に merge しようとすると `refusing to merge unrelated histories` と怒られ、
無理に繋ぐと**同じ内容のコミットが二重に並ぶ**。

**見分け方**

```bash
git fetch origin

# 自分の main とリモートの main がどれだけ離れているか
git rev-list --left-right --count origin/main...main
# 出力が「371  398」のように両方とも大きい → 分岐している

# 共通の祖先があるか
git merge-base origin/main main
# 何も返ってこない（エラーになる）→ 共通祖先が無い＝別履歴
```

普段の作業でも、**`git status` に `have diverged` と出たら黄色信号**。
自分のコミット数と相手のコミット数が両方 2 桁になっていたら、すぐ相談する。

**どうするか**

- **自分で解決しようとしない。** merge や force push を試すと、より複雑になる
- すぐにチームに共有し、**全員の push を一旦止めてもらう**
- 各自の未 push の作業をブランチとして退避してから、リーダーが統合する

**予防策**（これを守れば起きない）

1. リポジトリは **GitHub 側で 1 回だけ作る**。手元では `git clone` するだけで、`git init` はしない
2. origin を付け替えない
3. `push --force` を使わない

---

## 8. Issue で作業を管理する

Issue はテンプレートを用意しただけでは使われない。**WBS と結びつけると回り始める。**

### 8.1 WBS のタスクを Issue にする

[`meetings/wbs.md`](meetings/wbs.md) に書いたタスクを、そのまま Issue にする。

1. WBS の 1 行 = Issue 1 件で起票する
   （[`.github/ISSUE_TEMPLATE/task.md`](.github/ISSUE_TEMPLATE/task.md) を使うと書式が揃う）
2. Issue のタイトル先頭に **WBS 番号**を付ける → 例: `[WBS-07] センサ読み取りを実装する`
3. WBS の表に、発行された **Issue 番号を書き戻す**

| # | タスク | 担当 | 状態 | Issue |
|---|---|---|---|---|
| 7 | センサ読み取りを実装 | A | 進行中 | #12 |

これをやらないと、WBS と GitHub が別々に管理されて片方が必ず腐る。
**「WBS を見れば Issue に飛べる」状態を保つのが目的。**

### 8.2 Issue → ブランチ → PR をつなぐ

```bash
# Issue #12 に着手するとき
git switch main && git pull
git switch -c feature/12-sensor-reading   # ブランチ名に Issue 番号を入れる
```

- コミットメッセージにも Issue 番号を入れる → `[機能追加] センサ読み取りを実装 (#12)`
- PR の説明文に **`Closes #12`** と書くと、マージ時に自動で Issue が閉じる

こうしておくと、Issue のページに「どのブランチ・どの PR・どのコミットで対応したか」が
全部ぶら下がる。**後から「あの変更なんでやったんだっけ」を追える**のが最大の利点。

### 8.3 うまく回らないときは

Issue を使うこと自体が目的ではない。3 週間使ってみて誰も起票しないなら、
**チームで話して運用を変えてよい**（GitHub Projects のボードにする、
ミーティングで口頭確認にする等）。放置された仕組みがいちばん害になる。

---

## 9. リポジトリに入れてよいもの・ダメなもの

冒頭に書いたとおり、このリポジトリは**「いつ public になっても平気」な状態**で運用する。

### 9.1 線引き表

| 種類 | どちらへ |
|---|:---:|
| ソースコード・ビルド設定・CI 設定 | **public**（このリポジトリ） |
| 設計書・データフォーマット定義・ADR | **public** |
| 開発の運用規約（このファイルなど） | **public** |
| チームの提出物（報告書の `.tex` と PDF） | **public** |
| 自分たちで撮った回路写真・自作した図 | **public** |
| 個人レポート・個人提出物（氏名・学籍番号入り） | **private** |
| 授業の配布資料（スライド・課題 PDF・ルーブリック）＝著作権物 | **private** |
| 評価・成績に関わるもの | **private** |
| 議事録・チーム内の連絡・雑多なメモ | **private** |
| 人が写った写真・学内システムの画面キャプチャ | **private** |
| API キー・トークン・Wi-Fi のパスワード | **どちらにも入れない**（環境変数か `.env`） |

**判断ルール: 氏名・学籍番号・著作権物・評価情報が 1 つでも入るなら private。
公開されて困るか 5 秒迷ったら private。**

詳しい理由と、private 側リポジトリの作り方は
[`docs/before_coding.md`](docs/before_coding.md) にある。

### 9.2 コミット前の確認

`git add` したあと、`git commit` する前に**何を入れようとしているか目で見る**。

- **VS Code**: ソース管理タブで「ステージされた変更」のファイルを 1 つずつクリックして差分を見る
- **ターミナル**: `git status` と `git diff --staged`

とくに次の 3 つは見落としやすい。

1. **ファイル名**に学籍番号・実名が入っていないか
2. **スクリーンショット**にユーザー名・メールアドレス・ホームフォルダのパスが写っていないか
3. ツールが勝手に作ったフォルダ（`.venv/` `node_modules/` `.pio/` など）が混ざっていないか
   → 混ざっていたら、コミットする前に `.gitignore` に足す

### 9.3 機械のチェックを使う

手元でいつでも実行できる。

```bash
bash .github/scripts/secret_scan.sh
```

学籍番号らしき文字列・メールアドレス・API キー・絶対パスなどを検知する。
push すると CI（[Secret Scan](.github/workflows/secret-scan.yml)）でも同じ検査が走る。

**最初にやること**: [`.github/secret-scan-patterns.txt`](.github/secret-scan-patterns.txt) の
学籍番号のパターンを、自分たちの大学の形式に合わせて書き換える。

### 9.4 入れてしまったら

**push する前に気づいた場合**は、まだローカルだけなので直せる。慌てず、
コミットをやり直す方法をチームで確認してから作業する。

**push してしまった場合**は、自分で消そうとしない。

- **すぐにチームとリーダーに共有する**（黙って直すのがいちばん危険）
- 認証情報（トークン・パスワード）なら、**まずその鍵を無効化する**。
  履歴から消すより先に、漏れた鍵を使えなくするほうが重要
- 履歴から消すには全員の作業を止める必要があるので、判断はチームでする

---

## 10. 困ったら

- まずは PR / Issue に**質問を書く**（誰かが答えてくれる）
- Slack / Discord などチームのコミュニケーションツールで聞く
- `git` は取り返しのつくこと・つかないことがある。破壊的操作（`reset --hard` /
  `push --force` / `rebase`）は不安なら相談してから実行する
