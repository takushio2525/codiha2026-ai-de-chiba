# CHIZUBA 仕組みと構成の仕様書

このディレクトリは、**CHIZUBA のコードを読まずに「どこに何があって、どういう仕組みで、
どこと繋がっているか」を説明できるようになる**ための文書。

想定読者は**コードを書いていない人**（発表者・審査員からの質問に答える人）。
専門用語は初出で 1 行説明を付ける。すべての記述に**ファイルパス（必要なら行番号）・SQL・
実測値・出典 URL のいずれか**を添えてある。分からなかったものは「未確認」と明記する。

- 利用者向けの操作手順は [`docs/manual/`](../manual/README.md)
- 何を作るかの正本は [`docs/design/requirements.md`](../design/requirements.md)
- つなぎ目（API の形）の正本は [`docs/design/interfaces.md`](../design/interfaces.md)
- 提出要件の正本は `課題/2026-09-09_CODIHA2026_提出要件.md`

この仕様書は**コードから起こしたもの**なので、正本と食い違ったら正本を勝たせ、
この文書を直す。

---

## 目次

| # | ファイル | 内容 |
|---|---|---|
| 01 | [全体像と接続図](01-overview.md) | 何が何と繋がっているか。コンテナ・外部サービス・依存の向きを図で |
| 02 | [起動から画面表示まで](02-boot.md) | `docker compose up` から最初の 1 画面が出るまでを順番に |
| 03 | [ファイル全一覧](03-files.md) | 追跡ファイル 269 件すべて。役割・主要 export・**誰が import し誰に import されるか** |
| 04 | [機能ごとの処理フロー](04-features.md) | F-1〜F-8 を「クリックしてから結果が出るまで」で追う |
| 05 | [データベース](05-database.md) | 全テーブル・全カラム・型・制約・索引・`details` の中身 |
| 06 | [認証と権限](06-auth.md) | Auth.js の設定・JWT の中身・公開 URL の導出・API ごとの認可表 |
| 07 | [地図](07-map.md) | MapLibre の初期化・レイヤーの積み順・worker の罠・タイル・凡例 |
| 08 | [外部 API](08-external-apis.md) | エンドポイント・呼び出し頻度・キャッシュ・失敗時の挙動（実レスポンス付き） |
| 09 | [データパイプライン](09-data-pipeline.md) | 元データ → 加工スクリプト → 同梱ファイル → アプリの読み込み箇所 |
| 10 | [環境変数](10-config.md) | 全変数・必須/任意・既定値・未設定時の挙動・読まれる場所 |
| 11 | [設計判断とその理由](11-decisions.md) | なぜ MapLibre か・なぜ PostgreSQL か・なぜ予測と言わないか 等 |
| 12 | [テストと品質保証](12-quality.md) | 品質ゲートの実態（**自動テストは無い**）・CI・提出物検証 |
| 13 | [既知の制約・未実装・技術的負債](13-limitations.md) | 過少申告も過大申告もせず列挙 |
| 14 | [想定質問と回答（40 問）](14-faq.md) | 審査員が聞きそうな技術質問と、根拠付きの答え |
| 99 | [出典検証台帳](99-sources.md) | 引用した外部 URL を実取得して本文照合した記録 |

---

## 最初に押さえる 10 個の数字

質疑で最も聞かれる粒度の数字を先にまとめる。根拠は各章にある。

| 項目 | 値 | 根拠 |
|---|---|---|
| コンテナ数 | **2**（`web` = Next.js / `db` = PostgreSQL 17） | `app/compose.yaml:25-93` |
| 地図に載る施設の点 | **避難場所 123 / AED 304 / 子育て施設 388 / 景観100選 100** | `app/public/data/*.geojson` を実カウント |
| 景観スポットの写真 | **100 か所中 54 か所** | `app/public/images/scenic/` に 54 ファイル |
| デモ投稿 | **22 件**（危険 7・浸水 5・観光 10）＋ 写真 17 枚・コメント 6 件 | `app/db/init/003_seed_demo_reports.sql` |
| 投稿の上限 | タイトル 60 字 / 本文 1000 字 / コメント 500 字 / 写真 3 枚・1 枚 5 MB・1 送信 10 MB | `app/src/lib/reports.ts:151-158` |
| DB の同時接続 | プール上限 **10**、接続待ち 5 秒、1 クエリ 10 秒で打ち切り | `app/src/lib/db.ts:23,25,40` |
| 認証キーが要る外部サービス | **0 個**（Google OAuth のみ任意。未設定ならデモログイン） | `app/src/lib/authMode.ts:36-41` |
| 外部 API の呼び出し制限 | OSRM は **1 秒 1 リクエスト**（`MIN_INTERVAL_MS = 1_100`）、気象庁はキャッシュ 10/30 分 | `api/routing/route.ts:24`・`lib/jma.ts:43-47` |
| 自動テスト | **0 本**（テストフレームワーク未導入。品質ゲートは型検査・ビルド・秘匿情報スキャン・提出物検証の 4 本） | [12 章](12-quality.md) |
| 対応範囲 | 千葉県全域（市町村マスタに 1 行足すだけ）。デモデータは市川市 `12203` | `app/db/init/002_seed_municipalities.sql` |

---

## この文書の書き方（変更するときの約束）

1. **推測を書かない。** 実際にファイルを開き、実行し、`curl` で取得した結果だけを書く。
   確かめられなかったものは「未確認」と書く
2. **「どこにある」は実パスで書く。** 行番号は変わりうるので、
   併せて関数名・定数名など grep できる手がかりを添える
3. **「どういう仕組み」には必ず「なぜその仕組みか」を付ける。**
   理由の出どころは、コードのコメント・`.agent/architecture.md`・PR 本文・git log
4. **「どこと繋がる」は双方向に書く。** 呼ぶ側の項と呼ばれる側の項の両方に書く
5. 外部資料に言及したら **URL を付け、[99 章](99-sources.md)の台帳に追記する**
